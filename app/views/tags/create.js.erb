$('#formTag').removeClass('slided');

$('.mod-tags-index__list').append('<%= j render @tag %>');

$('#messageTag').show();

var tagId = '<%= @tag.id %>';
var tagName = '<%= @tag.name %>';

$('#tagNew').html(tagId);
$('#nameTag').html(tagName);

$('input#tag_name').val('');

setTimeout(
  function() 
  {
    $('#messageTag').fadeOut();
}, 2000);