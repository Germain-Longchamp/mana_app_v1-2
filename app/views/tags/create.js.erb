$('#renderTags').append('<%= j render @tag %>');
$('#messageTag').show();

$('#formTag').removeClass('slided');

var tagId = '<%= @tag.id %>';
var tagName = '<%= @tag.name %>';

$('#tagNew').html(tagId);
$('#nameTag').html(tagName);

$('input#tag_name').val('');

setTimeout(
  function() 
  {
    $('#messageTag').fadeOut();
}, 2000);