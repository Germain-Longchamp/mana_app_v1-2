<%= simple_form_for @engine do |f| %>

  <% if engine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(engine.errors.count, "error") %> prohibited this engine from being saved:</h2>

      <ul>
        <% engine.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :name, :label => "Nom/Marque" %>
  <%= f.input :internal_name, :label => "Nom interne" %>

  <%= f.association :company, as: :select, collection: Company.order(:id), :label => "Rattachée à l'entreprise" %>
  
  <%= f.input :tag_ids, as: :select, collection: Tag.order(:name), input_html: {multiple: true}, :label => "Famille" %>

  <%= f.association :room, as: :select, collection: Room.order(:id), :label => "Salle affectée" %>

  <%= f.input :initial_date, as: :date, html5: true %>

  <%= f.rich_text_area :description, :label => "Informations supplémentaires" %>

  <%= f.submit "Mettre à jour", class: 'btn btn-lg btn-primary float-right' %>
<% end %>