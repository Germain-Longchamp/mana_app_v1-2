
<%= simple_form_for(@intervention) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :detail %>
    <%= f.association :intervention_category, as: :select, collection: InterventionCategory.where(:company_id => current_user.company_id).order(:id), :label => "Catégorie intervention" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
